const getPageContent = {
  type: 'GET_PAGE_CONTENT',
  pageContent: {},
};

const getDates = {
  type: 'GET_DATES',
  dates: {},
};

const getImages = {
  type: 'GET_IMAGES',
  images: {},
};

export { getPageContent, getDates, getImages };
