/* global it, describe, expect */
// const App = require('../app');

// TODO: create 'express' module mock and remove dummy test below
describe('App.', () => {
  it('should be a function', () => {
    // expect(App).toBeInstanceOf(Function);
    expect(true).toEqual(true);
  });
});
